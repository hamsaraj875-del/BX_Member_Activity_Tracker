{
  "name": "bx-analytics-root",
  "version": "1.0.0",
  "private": true,
  "description": "BX Member Activity Tracking & Analytics Platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "build": "npm run build:frontend && npm install --prefix backend",
    "start": "node backend/server.js"
  }
}
